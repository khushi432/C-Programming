/*
Operator	Name	 Example	Description	
   ==	  Equal to	  x == y	Returns 1 if the values are equal	and 0 if not equal to

*/

#include <stdio.h>
int main()
{
    int a = 12;
    int b = 13;
    printf("a and b equal hai ? Type 1 if Yes else 0:=>  %d\n", a==b);
}