/*
Operator	Name	 Example	Description	
   >	Greater than	x > y	Returns 1 if the first value is greater than the second value

*/

#include <stdio.h>
int main()
{
    int a = 15;
    int b = 13;
    printf("a and b Greater than ? Type 1 if Yes else 0:=>  %d\n", a>b);
}