/*
Operator	Name	 Example	Description	
    <	Less than	x < y	Returns 1 if the first value is less than the second value
*/

#include <stdio.h>
int main()
{
    int a = 15;
    int b = 16;
    printf("a and b Less than ? Type 1 if Yes else 0:=>  %d\n", a<b);
}